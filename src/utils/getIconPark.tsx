// import IconPark, { ALL_ICON_KEYS, IconType } from '@icon-park/react/es/all';

// export const getIconPark = (iconName: IconType, props = {}) => {
//   if (!iconName || !ALL_ICON_KEYS.includes(iconName)) {
//     return null;
//   }
//   return <IconPark type={iconName} {...props} />;
// };

// export default getIconPark;
